name: My First Pipeline

on:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Code checkout
        uses: actions/checkout@v3

      - name: First step
        run: echo "Pipeline chalু hoyeche!"

      - name: Second step
        run: echo "Test pass hoyeche!"
